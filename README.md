Ove komande služe za postavljanje vaše aplikacije na GitHub. Zamenite URL vašim repozitorijumom po potrebi.

```bash
git init -b main
git add .
git commit -m "Prva verzija CopyCalc aplikacije"
git remote add origin https://github.com/CopaBGDev/copyshopcalc.git
git push -u origin main
```

# Firebase Studio

This is a NextJS starter in Firebase Studio.

To get started, take a look at src/app/page.tsx.
